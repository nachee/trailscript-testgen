# Generated E2E Tests — {{ site_domain }}

Generated by TrailScript from {{ total_sessions }} recorded user sessions.
{% if dedup_strategy %}

**Dedup strategy**: {{ dedup_strategy }}{% if original_flow_count and original_flow_count != flow_count %} ({{ original_flow_count }} flows → {{ flow_count }} test files){% endif %}

{% endif %}

## Test Files: {{ flow_count }}

{% for flow in flows %}
- **{{ flow.name }}** ({{ flow.popularity_score }}% popularity){% if flow.test_mode is defined and flow.test_mode != 'full' %} — _{{ flow.test_mode | replace('_', '-') }}_{% endif %} — `{{ flow.file_name }}`
{% endfor %}

## Quick Start

```bash
npm install
npx playwright install
npx playwright test
```

## Variables

Edit `variables.ts` to set test-specific values (credentials, IDs, etc.).

## Notes

- Tests are generated from real user behavior patterns
- Selectors prioritise role-based and testid locators for stability
- Assertions are derived from DOM checkpoints captured during recording
